name: Sentry Release

# Controls when the workflow will run
on: 
  release:
    types: [published]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: getsentry/action-release@v1.2.0
